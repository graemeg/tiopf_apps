Console Commands for timap.exe.

-file [-f]

Specifies the .xml file that will be processed.  Any include of that file are also 
processed.

-verbose [-v]

Console emits more verbose information output of units, classes created.


EXAMPLE 1:

timap -v -f m:\path\to\my_schema.xml

